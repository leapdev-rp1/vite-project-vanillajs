import { MatterBasedRequest } from '.';
export declare namespace app {
    interface OpenAppRequest extends MatterBasedRequest {
        appId: string;
        groupId: string;
    }
    interface Api {
        openApp(request: OpenAppRequest): void;
    }
}
