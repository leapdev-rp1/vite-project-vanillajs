import { staff } from '../../api/staff';
import { BrowserFourdApi } from '.';
export declare class Staff implements staff.Api {
    private fourd;
    constructor(fourd: BrowserFourdApi);
    selectStaff(request?: staff.SelectStaffRequest): Promise<staff.Staff[]>;
}
