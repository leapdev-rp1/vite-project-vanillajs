import { app } from '../../api/app';
import { IframeWindow } from '.';
export declare class App implements app.Api {
    private iframe;
    constructor(iframe: IframeWindow);
    openApp(request: app.OpenAppRequest): void;
}
